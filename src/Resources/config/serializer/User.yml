App\Entity\User:
  properties:
    id:
      groups: ['service', 'auth']
    firstName:
      groups: ['auth']
    image:
      groups: ['service']
    lastName:
      groups: ['auth']
    email:
      groups: ['auth']
    roles:
      groups: ['auth']
