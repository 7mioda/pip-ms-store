<!DOCTYPE html>

<title>New Like</title>

{% block body %}
    <h1>Create new Like</h1>

    {{ include('like/_form.html.twig') }}

    <a href="{{ path('like_index') }}">back to list</a>
{% endblock %}
