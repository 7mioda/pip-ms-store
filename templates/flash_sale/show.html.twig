<!DOCTYPE html>

<title>FlashSale</title>

{% block body %}
    <h1>FlashSale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flash_sale.id }}</td>
            </tr>
            <tr>
                <th>BeginDate</th>
                <td>{{ flash_sale.BeginDate ? flash_sale.BeginDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ flash_sale.endDate ? flash_sale.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ flash_sale.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('flash_sale_index') }}">back to list</a>

    <a href="{{ path('flash_sale_edit', {'id': flash_sale.id}) }}">edit</a>

    {{ include('flash_sale/_delete_form.html.twig') }}
{% endblock %}
