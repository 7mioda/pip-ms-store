<!DOCTYPE html>

<title>Edit FlashSale</title>

{% block body %}
    <h1>Edit FlashSale</h1>

    {{ include('flash_sale/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('flash_sale_index') }}">back to list</a>

    {{ include('flash_sale/_delete_form.html.twig') }}
{% endblock %}
